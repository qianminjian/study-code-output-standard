<!--
@meta {
  "id": "04-后端-Mapper操作清单",
  "version": "0.1.0",
  "last_updated": "<YYYY-MM-DD>",
  "data_source": "<来源>",
  "code_version": "<可选:git:<hash> | <unversioned> | 留空>",
  "owner": "<团队>",
  "ai_consumable": true,
  "severity_taxonomy": "P0|P1|P2|P3"
}
-->
<!-- @prompt: ../ai-prompts/04-后端-Mapper操作清单.md (单点真源，ai-prompts/04-后端-Mapper操作清单.md 是维护入口) -->
<!-- 上面 @prompt 引用 ai-prompts/ 目录下的同名文件。修改本模板时同步刷 ai-prompts/04-后端-Mapper操作清单.md。 -->

# 04 · 后端 Mapper 操作清单

> 共 N 个 Mapper 接口 + M 个 MyBatis XML 映射

## 1. 接口与 XML 一览

| Mapper 接口 | XML 文件 | 表/视图 | 业务域 |
|---|---|---|---|
| <XxxMapper> | <XxxMapper.xml> | `<table>` | <业务域> |

## 2. 通用基类

```java
<基类代码>
```

> **注意**：XML 中**没有**这些通用方法的实现！

## 3. 每个 Mapper 的方法清单

### 3.1 <XxxMapper>
- `findAll()` — <业务>
- `findPage()` — 分页
- `findByXxx(...)` — <业务>

## 4. 关键 SQL 模式

### 4.1 分页（反射约定）
```java
<示例>
```

### 4.2 多表 Join
```xml
<示例>
```

### 4.3 树形递归 resultMap
```xml
<示例>
```

### 4.4 派生字段
```xml
<示例>
```

## 5. ⚠️ 已知 Mapper 问题

| 严重度 | 文件 | 问题 |
|---|---|---|
| 🔴 P0 | <文件:行号> | <问题> |
| 🟡 P1 | <文件:行号> | <问题> |
| 🟢 P2 | <文件:行号> | <问题> |

## 元信息

| 字段 | 值 |
|---|---|
| 验证命令 | `find <mapper-dir> -name "*.java" \| wc -l` |
| 注入扫描 | `grep -rE "\\$\\{[^}]+\\}" <xml-dir>` |
