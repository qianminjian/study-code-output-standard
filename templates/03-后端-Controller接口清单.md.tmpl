<!--
@meta {
  "id": "03-后端-Controller接口清单",
  "version": "0.1.0",
  "last_updated": "<YYYY-MM-DD>",
  "data_source": "<来源>",
  "code_version": "<可选:git:<hash> | <unversioned> | 留空>",
  "owner": "<团队>",
  "ai_consumable": true,
  "severity_taxonomy": "P0|P1|P2|P3"
}
-->
<!-- @prompt: ../ai-prompts/03-后端-Controller接口清单.md (单点真源，ai-prompts/03-后端-Controller接口清单.md 是维护入口) -->
<!-- 上面 @prompt 引用 ai-prompts/ 目录下的同名文件。修改本模板时同步刷 ai-prompts/03-后端-Controller接口清单.md。 -->

# 03 · 后端 Controller 接口清单

> 共 N 个 Controller。所有路径前缀 `<prefix>`。
> v2.3 改进：§1-B 摘要表必填（满足 check-consistency 端点对齐），§2 详表可按 Controller 拆文件（如 `03-端点详情-<Controller>.md`）。

## 1. 概览

| Controller | 前缀 | Api tags | 端点数 | 公开端点 |
|---|---|---|---|---|
| <Controller> | <prefix> | <tags> | <N> | <N> |

> 标注 ⚠️ 的是 `@Api(tags=...)` 错误标签。

## 1-B. 端点摘要（v2.3 新增 · 必填 · check-consistency 对账）

> 本表是 check-consistency.sh 校验的"标准答案"——**必须与代码 grep 数对齐**。
> §2 详表可分文件，但**本摘要表所有数字必须精确**。

| 项目 | 数量 | 验证命令 | 状态 |
|---|:-:|---|:-:|
| Controller 总数 | <N> | `find <controller-dir> -name "*Controller.java" \| wc -l` | ⬜ |
| **端点总数**（方法级）| <N> | `grep -rE "@(Get\|Post\|Put\|Delete)Mapping" <controller-dir> \| wc -l` | ⬜ |
| 公开端点数 | <N> | `grep -rl "@CrossOrigin" <controller-dir> \| wc -l`（粗略）| ⬜ |
| 鉴权端点数 | <N> | `grep -rl "@PreAuthorize" <controller-dir> \| wc -l`（粗略）| ⬜ |
| 全局前缀 | <无/api/v1> | `grep -r "@RequestMapping(\"/" <controller-dir> \| head -1` | ⬜ |
| Swagger 启用 | <是/否> | `grep -r "@Api\|springfox\|springdoc" <controller-dir> \| head -1` | ⬜ |

> 校验不通过示例：expected=66 documented=19 → 03 需补充 47 个端点（v2.2 之前问题，v2.3 起需对齐）。

## 2. 详细接口清单

### 2.1 <Controller>（`<prefix>`）
| 方法 | 路径 | 入参 | 鉴权 | 公开 | 说明 |
|---|---|---|---|:-:|---|
| GET | `/path` | <入参> | <权限> | ✅/— | <说明> |
| POST | `/path` | <入参> | <权限> | ✅/— | <说明> |

> 详表内容可分文件：`asset-docs/03-端点详情-<Controller>.md`，本节保持精简。

## 3. 权限字符串全集（去重）

| 子系统 | 资源 | view | add | edit | delete | upload | download |
|---|---|---|---|---|---|---|---|
| <sys> | <res> | ✅ | ✅ | ✅ | ✅ | — | — |

> 共 N 个独立权限串，存在 N 处错用/复用：
> 1. <位置>: 错用 X
> 2. ...

## 4. 公开端点（无需 token）

| 端点 | 用途 | 备注 |
|---|---|---|
| `<path>` | <用途> | <备注> |

## 元信息

| 字段 | 值 |
|---|---|
| 验证命令 | `grep -rE "@(Get\|Post\|Put\|Delete)Mapping" <controller-dir> \| wc -l` |
| Swagger 校对 | 访问 `<swagger-url>` |
