# AI Prompt · 08 前端状态管理与路由

> ⚠️ **核心原则：按代码实际输出，不按模板预设清单**
>
> - **必须忠实还原代码中的实际状态管理/路由/API 模块**，从代码扫描，有哪些列哪些
> - **模块分类从代码实际目录结构出发**，不是按模板预设"状态管理/路由/API模块"三大块
> - 如果代码中没有 Vuex/Pinia，就不写"状态管理模块"这一节

---

## 完整 Prompt

```
【背景】
项目 <name>，前端框架 <Vue 2/Vue 3>，状态管理 <Vuex/Pinia/Redux>。本任务生成"状态管理与路由"资产。
通过逆向前端代码，推断状态管理和路由架构。

【核心原则】
⚠️ 不按模板预设清单输出，按代码实际扫描结果输出！
- 模块从 find/ls 命令扫描，有哪些列哪些
- 模块分类从实际目录结构出发，不是预设分类
- 没有某类模块，就不写那一节

【任务】
生成 asset-docs/08-前端-状态管理与路由.md。
严格按 ${SKILL_HOME}/assets/08-前端-状态管理与路由.md.tmpl 模板结构。

【输入】
- store/ 目录：src/store/ 或 src/stores/
- router/ 目录：src/router/
- http/ 或 api/ 目录：src/http/ 或 src/api/
- utils/ 目录：src/utils/
- main.js/ts 入口文件

【扫描命令（必跑）】
```bash
# Store 模块数
ls store/modules/ 2>/dev/null || ls store/ 2>/dev/null | wc -l

# API 模块数
ls src/api/ 2>/dev/null | wc -l

# 路由定义数
grep -c "path:" src/router/index.js

# Token 相关
grep -rn "token\|Token" src/store/
grep -rn "Authorization" src/
```

【输出字段·必须包含】

## 1. 状态管理架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      Vuex / Pinia Store                          │
├─────────────────────────────────────────────────────────────────┤
│  modules/                                                        │
│  ├── user.js          ← 用户状态、Token、权限                     │
│  ├── permission.js    ← 路由权限、菜单树                         │
│  ├── settings.js       ← 系统配置                               │
│  └── app.js           ← 全局状态（loading, theme）              │
├─────────────────────────────────────────────────────────────────┤
│  getters/           ← 计算属性                                  │
│  mutations/         ← 同步修改                                   │
│  actions/           ← 异步操作                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 1.1 用户状态模块（user）

```javascript
// store/modules/user.js
const state = {
  token: localStorage.getItem('token') || '',
  userInfo: null,
  permissions: [],
  roles: []
};

const mutations = {
  SET_TOKEN(state, token) {
    state.token = token;
    localStorage.setItem('token', token);
  },
  SET_USER_INFO(state, userInfo) {
    state.userInfo = userInfo;
  },
  SET_PERMISSIONS(state, permissions) {
    state.permissions = permissions;
  }
};

const actions = {
  async login({ commit }, loginForm) {
    const res = await login(loginForm);
    if (res.code === 0) {
      commit('SET_TOKEN', res.data.token);
      commit('SET_USER_INFO', res.data.userInfo);
      commit('SET_PERMISSIONS', res.data.permissions);
    }
    return res;
  },
  async logout({ commit }) {
    await logout();
    commit('SET_TOKEN', '');
    commit('SET_USER_INFO', null);
    commit('SET_PERMISSIONS', []);
  }
};
```

| 模块 | State | Mutations/Actions | 持久化 |
|---|---|---|---|
| user | token, userInfo, permissions | login, logout, setPerms | localStorage |
| permission | routes, dynamicRoutes | setRoutes, generateRoutes | 内存 |
| settings | theme, size | setTheme, setSize | localStorage |

## 2. Token 管理分析

### 2.1 Token 存储位置

| 存储方式 | 位置 | 风险 | 严重度 |
|---|---|---|---|
| localStorage | localStorage.token | XSS 可窃取 | 🟡P1 |
| sessionStorage | sessionStorage.token | 标签页关闭丢失 | 🟢P2 |
| Cookie (HttpOnly) | document.cookie | CSRF 风险 | 🟡P1 |
| Vuex/Pinia | 内存 | 刷新丢失，需持久化 | 🟡P1 |

### 2.2 Token 刷新机制

```javascript
// HTTP 拦截器
axios.interceptors.request.use(config => {
  const token = store.state.user.token;
  if (token) {
    config.headers['Authorization'] = `Bearer ${token}`;
  }
  return config;
});

// Token 刷新逻辑（需核实）
// ⚠️ 未找到 refresh_token 刷新逻辑
```

**⚠️ 问题识别**：
| 问题 | 位置 | 严重度 |
|---|---|---|
| Token 刷新机制缺失 | 未找到 refresh_token 逻辑 | 🔴P0 |
| Token header 非标准 | 用 token 而非 Authorization: Bearer | 🟡P1 |

## 3. 路由架构

### 3.1 路由表结构

```javascript
// router/index.js
const routes = [
  // 公开路由
  {
    path: '/login',
    component: () => import('@/views/login'),
    meta: { public: true }
  },
  // 异步路由（权限控制）
  {
    path: '/',
    component: Layout,
    redirect: '/dashboard',
    children: [
      {
        path: 'dashboard',
        component: () => import('@/views/dashboard'),
        meta: { title: '首页', icon: 'dashboard' }
      }
    ]
  }
];
```

### 3.2 动态路由生成

```javascript
// 路由守卫
router.beforeEach(async (to, from, next) => {
  const hasToken = store.getters.token;

  if (hasToken) {
    if (to.path === '/login') {
      next({ path: '/' });
    } else {
      const hasRoles = store.getters.roles && store.getters.roles.length > 0;
      if (hasRoles) {
        next();
      } else {
        try {
          const { roles } = await getUserInfo();
          const accessRoutes = await generateRoutes(roles);
          router.addRoutes(accessRoutes);
          next({ ...to, replace: true });
        } catch (error) {
          // Token 失效，跳转登录
        }
      }
    }
  } else {
    if (to.meta.public) {
      next();
    } else {
      next('/login');
    }
  }
});
```

| 路由类型 | 数量 | 说明 |
|---|---|---|
| 静态路由 | N | 公开路由，始终加载 |
| 动态路由 | N | 根据权限动态添加 |
| 404 路由 | 1 | 通配 |

## 4. HTTP 客户端封装

### 4.1 Axios 拦截器

```javascript
// utils/http.js
const service = axios.create({
  baseURL: process.env.VUE_APP_BASE_API,
  timeout: 30000
});

// 请求拦截器
service.interceptors.request.use(
  config => {
    const token = store.state.user.token;
    if (token) {
      config.headers['Authorization'] = 'Bearer ' + token;
    }
    return config;
  },
  error => {
    console.error('请求错误', error);
    return Promise.reject(error);
  }
);

// 响应拦截器
service.interceptors.response.use(
  response => {
    const res = response.data;
    if (res.code !== 0) {
      // 业务错误处理
      if (res.code === 401) {
        // Token 失效
        store.dispatch('user/logout');
      }
      return Promise.reject(new Error(res.message));
    }
    return res;
  },
  error => {
    // HTTP 错误处理
    if (error.response && error.response.status === 401) {
      store.dispatch('user/logout');
    }
    return Promise.reject(error);
  }
);
```

### 4.2 拦截器问题识别

| 问题 | 位置 | 严重度 | 建议 |
|---|---|---|---|
| 401 处理在响应拦截器 | utils/http.js | 🔴 | 应在请求前检查 token 有效期 |
| 无重试机制 | utils/http.js | 🟡 | 添加 axios-retry |
| 无统一错误提示 | utils/http.js | 🟡 | Element Message 统一提示 |

## 5. API 模块清单

| 模块 | API 数量 | 主要接口 | 对应 03 位置 |
|---|---|---|---|
| user.js | 5 | login, logout, getUserInfo, updateUser | UserController |
| role.js | 4 | list, create, update, delete | RoleController |
| permission.js | 3 | getTree, grant, getMyPerms | PermissionController |

### 5.1 API 调用示例

```javascript
// api/user.js
export function login(data) {
  return service.post('/user/login', data);
}

export function getUserInfo() {
  return service.get('/user/info');
}

export function updateUser(id, data) {
  return service.put(`/user/${id}`, data);
}
```

## 6. 权限控制实现

### 6.1 按钮级权限

```javascript
// 方式1: v-if + 权限判断
<el-button v-if="hasPermission('user:add')">新增</el-button>

// 方式2: 自定义指令
<el-button v-permission="['user:add']">新增</el-button>

// 全局指令定义
Vue.directive('permission', {
  inserted(el, binding) {
    const permissions = store.state.user.permissions;
    if (!permissions.includes(binding.value)) {
      el.parentNode.removeChild(el);
    }
  }
});
```

| 权限方式 | 实现 | 优缺点 |
|---|---|---|
| v-if | `hasPermission()` | ✅ 简单 ⚠️ 每个按钮都要写 |
| v-permission 指令 | 全局指令 | ✅ 可复用 ⚠️ 需要注册 |
| mixin | 混入方法 | ⚠️ 混入过多导致混乱 |

## 7. 状态持久化策略

| 数据 | 持久化位置 | 风险 |
|---|---|---|
| token | localStorage | ⚠️ XSS 风险 |
| userInfo | Vuex (内存) | ⚠️ 刷新丢失 |
| theme | localStorage | ✅ 无敏感数据 |
| 权限串 | Vuex (内存) | ⚠️ 刷新需重新获取 |

## 8. 已知状态管理问题

| 严重度 | 位置 | 问题 | 根因 |
|---|---|---|---|
| 🔴P1 | store/modules/user.js:42 | Token 刷新机制缺失 | 需求未明确 |
| 🟡P2 | utils/http.js:28 | 401 处理在拦截器而非路由守卫 | 临时方案 |
| 🟡P2 | router/index.js:15 | 动态路由生成无缓存 | 每次刷新重新加载 |
| 🟡P2 | store/settings.js | 主题切换无动画 | 需求简化 |

## 9. 未解之谜

| # | 问题 | 重要性 | 推断 |
|---|---|:-:|---|
| 1 | Token 过期时间是多少？ | 🔴 | 后端配置未找到 |
| 2 | Token 刷新机制如何？ | 🔴 | 未找到 refresh_token 逻辑 |
| 3 | 多标签页如何同步状态？ | 🟡 | 需核实 BroadcastChannel/localStorage |

【元信息】
| 字段 | 值 |
|---|---|
| 配套 | 03-Controller接口清单 / 07-前端页面 |
| Store 模块数 | N |
| API 模块数 | N |
| 路由数 | N |

【强制要求】
- Token 管理必须分析安全性
- HTTP 拦截器必须列出
- 权限控制实现必须详解
- 动态路由生成必须分析

【自我校验】
- Store 模块数与文件数一致
- API 与后端 03 完全对齐
- Token 安全性已评估
- 拦截器问题已记录
```
