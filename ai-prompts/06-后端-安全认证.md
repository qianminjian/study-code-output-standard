# AI Prompt · 06 安全认证

## 完整 Prompt

```
【背景】
项目 <name>，安全框架 <Spring Security/Shiro/...>。本任务生成"安全/认证/AOP/Config"资产。

【任务】
生成 asset-docs/06-后端-安全认证.md。
按 methodology/03-文档模板与质量标准.md §06 模板。

【输入】
- Security 配置类
- 鉴权 Filter / Interceptor
- AOP 切面（@Aspect）
- @Configuration 类（CORS、文件上传、跨域等）
- Utils 工具类
- application*.properties / yml

【输出字段】
1. <Security 框架> 配置（含代码片段）
2. 认证全流程（签发 / 过滤 / 校验 / 加载）
3. AOP 切面（含未触发的死切面）
4. Config 类清单
5. Utils 工具类
6. 多环境配置
7. ⚠️ 安全风险清单

【OWASP Top 10 对照】
- A01 访问控制失效：权限字符串错用
- A02 加密失败：密钥/密码硬编码
- A03 注入：SQL 注入 / XSS
- A04 不安全设计：白名单缺失
- A05 安全配置错误：CORS 通配 / actuator 暴露
- A06 危险组件：版本落后
- A07 鉴权失败：JWT 过期无刷新
- A08 数据完整性失败：缺签名
- A09 日志失败：SysLogAspect 未触发
- A10 SSRF：URL 拼接

【强制列】
- Config：| 类 | 作用 |
- Utils：| 工具 | 方法 | 用途 |
- 风险：| 严重度 | 问题 | 位置 |

【关键发现】
- 密钥硬编码（`"hardcoded-default-secret"` 类）
- CORS allowedOrigins("*") + allowCredentials(true)
- /actuator 全部暴露
- AOP Pointcut 写错包名导致未触发
- findUserByPassword 走明文路径

【反例】
- 密钥/token/密码**必须**用 <REDACTED> 替代
- 不漏 actuator 暴露
- 不漏 CORS 通配
- 不漏死切面

【自我校验】
- 密钥未明文出现（用 grep 验证）
- 元信息头 7 字段
- 风险 ≥ 5 条
```

## 校验清单

- [ ] 无明文密钥/密码
- [ ] OWASP 10 类对照
- [ ] 风险清单 ≥ 5
- [ ] AOP 切面含触发验证
- [ ] 多环境差异完整
