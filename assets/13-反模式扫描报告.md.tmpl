<!--
@meta {
  "id": "13-反模式扫描报告",
  "version": "0.1.0",
  "last_updated": "<YYYY-MM-DD>",
  "data_source": "<scan-antipatterns.sh 扫描结果 + 人工补充>",
  "code_version": "<可选:git:<hash> | <unversioned> | 留空>",
  "owner": "<团队>",
  "ai_consumable": true,
  "severity_taxonomy": "P0|P1|P2|P3"
}
-->
<!-- @prompt: 无 (13 号资产由 scan-antipatterns.sh 脚本派生，无独立 prompt) -->
<!-- 13 号资产由 Step 5 scan-antipatterns.sh 脚本自动生成，不从 references/prompts/ 派生。 -->

# 13 · 反模式扫描报告

> v2.2 新增。**本资产专注反模式分布与降级建议**——业务向的修复清单见 `11-技术债与遗留项.md`。
> 扫描工具：`${SKILL_HOME}/scripts/scan-antipatterns.sh`（v2.3 满覆盖 24/24 标签）。

## 1. 24 标签扫描矩阵

| 标签 | 严重度 | 命中数 | 位置 | 状态 |
|---|:-:|:-:|---|:-:|
| `@sqlinjection` | P0 | <N> | <XML 位置> | 🔴/🟢/🟡 |
| `@secret-leak` | P0 | <N> | <Java 位置> | 🔴/🟢 |
| `@cors-wildcard` | P0 | <N> | <Java 位置> | 🔴/🟢 |
| `@actuator-exposure` | P0 | <N> | <SecurityConfig> | 🔴/🟢 |
| `@wrong-token-hdr` | P0 | <N> | <HttpUtil> | 🔴/🟢 |
| `@xss` | P1 | <N> | <Vue .html()> | 🟡/🟢 |
| `@csrf` | P1 | <N> | <SecurityConfig.csrf().disable()> | 🟡 |
| `@perm-mismatch` | P1 | <N> | <Vue perms=> | 🟡/🟢 |
| `@admin-bypass` | P1 | <N> | <Login.vue default> | 🟡/🟢 |
| `@null-pointer` | P1 | <N> | 静态分析 | 🟡 |
| `@missing-tx` | P1 | <N> | <Service 缺 @Transactional> | 🟡/🟢 |
| `@wrong-default` | P1 | <N> | <默认值> | 🟡/🟢 |
| `@n+1` | P2 | <N> | <XML 子查询> | 🟢 |
| `@hardcoded` | P2 | <N> | <硬编码 IP> | 🟢 |
| `@copy-paste` | P2 | <N> | <重复代码> | 🟢 |
| `@wrong-tag` | P2 | <N> | <@Pointcut 写错> | 🟢 |
| `@dead-code` | P3 | <N> | <空 class/method> | ⚪ |
| `@todo-stub` | P3 | <N> | <TODO Auto-generated> | ⚪ |
| `@magic-number` | P3 | <N> | <== 数字> | ⚪ |
| `@long-function` | P3 | <N> | <LOC > 100> | ⚪ |
| `@long-param` | P3 | <N> | <形参 > 5> | ⚪ |
| `@feature-envy` | P3 | <N> | 跨文件 | ⚪ |
| `@shotgun-surgery` | P3 | <N> | 跨文件 | ⚪ |
| `@wrong-package` | P3 | <N> | <包名错位> | ⚪ |
| `@missing-i18n` | P3 | <N> | <中英文 i18n 数量> | ⚪ |

> 详细位置见 `${SKILL_HOME}/scripts/scan-antipatterns.sh` 实际输出。

## 2. 严重度热图

```
P0 (24h):    <N> 个  @sqlinjection / @secret-leak / @cors-wildcard / @actuator-exposure / @wrong-token-hdr
P1 (季度):   <N> 个  @xss / @csrf / @perm-mismatch / @admin-bypass / @null-pointer / @missing-tx / @wrong-default
P2 (半年):   <N> 个  @n+1 / @hardcoded / @copy-paste / @wrong-tag
P3 (顺便):  <N+> 个  @todo-stub / @dead-code / @magic-number / @long-function / @long-param / @feature-envy / @shotgun-surgery / @wrong-package / @missing-i18n
```

## 3. 已知 false positive（人工标记）

| 标签 | 位置 | 原因 |
|---|---|---|
| @secret-leak | <位置> | <变量赋值，非真泄漏> |
| @hardcoded | <位置> | <test 文件> |
| ... | ... | ... |

## 4. 已知漏检（扫描未覆盖 / 边界）

| 漏检项 | 影响 | 建议增强 |
|---|---|---|
| 启动时切面 `Mapped` 行验证 | @wrong-tag 漏检 | CI 加 `mvn spring-boot:run` 启动日志 |
| 完整 SQL 注入（`${}` 在注释/字符串中）| @sqlinjection 误报 | 用 AST 工具 |
| 跨文件复制粘贴 | @copy-paste 漏检 | 集成 simian/jscpd |
| 真实空指针 | @null-pointer 漏检 | 集成 sonar/spotbugs |

## 5. 降级建议

1. **scan 脚本增强**：加 8 类扫描（@wrong-package / @n+1 / @copy-paste / @magic-number 等）→ 已 v2.3 完成
2. **白名单**：排除 `*/test/*` `*/target/*` 减少 false positive → 已 v2.3 完成
3. **切面验证**：CI 加 step 启动后 `Mapped` 行 + 切面日志确认 → 待 v2.4
4. **人工 review**：P0/P1 必须人工二次确认（脚本仅做"候选名单"）

## 元信息

| 字段 | 值 |
|---|---|
| 扫描工具版本 | scan-antipatterns.sh v2.3（24/24 满覆盖）|
| 扫描时间 | <YYYY-MM-DD> |
| 命中率 | <N>/24 = <%> |
| 漏检率 | <N>/24 = <%> |
| code_version | <可选:git:<hash> \| <unversioned> \| 留空> |
