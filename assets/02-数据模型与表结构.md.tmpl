<!--
@meta {
  "id": "02-数据模型与表结构",
  "version": "0.1.0",
  "last_updated": "<YYYY-MM-DD>",
  "data_source": "<来源>",
  "code_version": "<可选:git:<hash> | <unversioned> | 留空>",
  "owner": "<团队>",
  "ai_consumable": true,
  "severity_taxonomy": "P0|P1|P2|P3"
}
-->
<!-- @prompt: ../references/prompts/02-数据模型与表结构.md (单点真源，references/prompts/02-数据模型与表结构.md 是维护入口) -->
<!-- 上面 @prompt 引用 references/prompts/ 目录下的同名文件。修改本模板时同步刷 references/prompts/02-数据模型与表结构.md。 -->

# 02 · 数据模型与表结构

> 共 N 个 Model + M 个 Mapper + K 个 XML + J 张表

## 1. 实体类与表对应关系

| Model | 对应表 | 继承 | 关键字段 | 备注 |
|---|---|---|---|---|
| **<BaseModel>** | — | — | <字段> | 父类 |
| **<Entity1>** | `<table>` | BaseModel | <字段> | <备注> |
| **<Entity2>** | `<table>` | BaseModel | <字段> | <备注> |

> 注意点：
> - <命名不一致>
> - <id 类型不一致>
> - <其它>

## 2. 数据库表清单

| 表名 | 中文 | 主要字段 | 索引/约束 |
|---|---|---|---|
| `<table1>` | <中文> | <字段> | <索引> |
| `<table2>` | <中文> | <字段> | <索引> |

## 2-B. ⚠️ 无 DDL 时的推断模式（可选 · 降级）

> **何时启用**：项目**没有** `*.sql` / `flyway/` / `liquibase/` / `db-scripts/` 等 DDL 来源时启用本段；**同时** §2 「数据库表清单」标 N/A。
> **典型场景**：老项目（wxcbrc、legacy Web）只有 XML + Model，无建表脚本。

**推断源（按可信度从高到低）**：

1. **MyBatis XML 的 resultType / parameterType**（最可信）—— grep `<resultMap>` 和 `resultType="<entity>"` 拿表/字段映射
2. **ORM 实体的 `@Table` / `@Entity` 注解**（Java JPA / Hibernate）
3. **Mapper 接口方法签名**（参数 + 返回值类型）
4. **前端 `http/modules/*.js` 中的字段名**（最弱，仅做交叉验证）

**强制标记**：所有推断出的表，**「表名」列前加 ⚠️**，并在 §元信息 表中加一行"DDL 来源：无（推断）"。

**示例**：

| ⚠️ 表名（推断） | 中文 | 主要字段 | 索引/约束 |
|---|---|---|---|
| ⚠️ `<table_from_resultType>` | 推断：<根据 Model 名> | 推断：<从 resultMap> | ⚠️ 未知 |
| ⚠️ `<table_from_xml_namespace>` | 推断：<根据 Mapper> | 推断：<从 SQL> | ⚠️ 未知 |

> **为什么独立成段**：与 §2 「真实 DDL」隔离，强制读者意识到这是推断，不是事实。CI 校验脚本会扫 ⚠️ 标记，提示"是否补充 DDL"。

## 3. 关键关联关系

### 3.1 权限模型
```
<ER 关系图>
```

### 3.2 业务模型
```
<ER 关系图>
```

### 3.3 字段命名约定
- 表字段：小写 + 下划线
- Java Model：小驼峰
- MyBatis：自动转换
- <其它约定>

## 4. MyBatis XML 命名空间

| XML 文件 | namespace | 主要操作 |
|---|---|---|
| `<XxxMapper.xml>` | `<namespace>` | <操作列表> |

## 5. 关键 SQL 模式

### 5.1 分页
- **Mapper 接口**：约定方法名 `findPage`
- **Service 实现**：反射执行
- **XML**：`<where>` + `<if>` 拼接

### 5.2 多表 Join
```xml
<示例 SQL>
```

### 5.3 ⚠️ SQL 注入风险
- <位置>: 使用 `${}` 字符串拼接

### 5.4 子查询 / N+1
- <位置>: N+1 风险

### 5.5 resultMap
- <位置>: 自定义 resultMap

## 元信息

| 字段 | 值 |
|---|---|
| 验证命令 | `find <model-dir> -name "*.java" \| wc -l` |
| SQL 注入扫描 | `grep -rE "\\$\\{[^}]+\\}" <xml-dir>` |
